---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  no_log: "{{ molecule_no_log }}"

  vars:
    aws_region: "af-south-1"
    keypair_name: "afs1-hakkutu-dev-ansible-role-openscap"
    security_group_name: "afs1-hakkutu-dev-ansible-role-openscap"

  tasks:
    - name: Ensure that instances are destroyed
      notify:
        - Cleanup resources
      amazon.aws.ec2_instance:
        name: "afs1-hakkutu-dev-ansible-role-openscap-*"
        state: absent
        region: "{{ aws_region }}"
        wait: true
        wait_timeout: 600

  handlers:
    - name: Ensure that a security group is deleted
      listen:
        - Cleanup resources
      amazon.aws.ec2_security_group:
        name: "{{ security_group_name }}"
        state: absent
        region: "{{ aws_region }}"

    - name: Ensure that an AWS keypair is deleted
      listen:
        - Cleanup resources
      amazon.aws.ec2_key:
        name: "{{ keypair_name }}"
        state: absent
        region: "{{ aws_region }}"

    - name: Populate instance config
      listen:
        - Cleanup resources
      ansible.builtin.set_fact:
        instance_conf: {}

    - name: Dump instance config
      listen:
        - Cleanup resources
      ansible.builtin.copy:
        content: |
          # Molecule managed

          {{ instance_conf | to_json | from_json | to_yaml }}
        dest: "{{ molecule_instance_config }}"
        mode: "0600"
...
